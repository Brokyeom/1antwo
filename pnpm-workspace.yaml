# pnpm 10+ 는 의존성의 postinstall 빌드 스크립트를 명시적으로 승인해야 실행한다.
# 승인 목록이 없으면 CI의 `pnpm install --frozen-lockfile`이 ERR_PNPM_IGNORED_BUILDS로 실패한다.
allowBuilds:
  "@firebase/util": true
  protobufjs: true
  sharp: true
  unrs-resolver: true
