{
  "rules": {
    ".read": false,
    ".write": false,
    "dashboard": {
      ".read": "auth != null && auth.token.email_verified == true && (auth.token.email.toLowerCase() == 'member1@example.com' || auth.token.email.toLowerCase() == 'member2@example.com')",
      ".write": "auth != null && auth.token.email_verified == true && (auth.token.email.toLowerCase() == 'member1@example.com' || auth.token.email.toLowerCase() == 'member2@example.com')"
    }
  }
}
